# Length-extrapolation experiment: Randomized YaRN + entropy invariance.
# model.name differs from the baseline run so checkpoints and TB logs stay separate —
# the two are not resume-compatible (vanilla RoPE stores rotary_embed.inv_freq, YaRN does not).
#
# Launch with bf16 — fp32 only fits 2400 frames/GPU on 24 GB:
#   PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True \
#   accelerate launch --mixed_precision bf16 src/wavtts/train/train.py --config-name WavTTS.yaml

hydra:
  run:
    dir: ckpts/${model.name}_${datasets.name}/${now:%Y-%m-%d}/${now:%H-%M-%S}

seed: 666  # run-level reproducibility: seeds python/torch/cuda and dataset shuffling

datasets:
  name: LibriTTS_100  # prepared data dir name under data/ (wav-only, 53.7 h)
  batch_size_per_gpu: 3200  # single 24 GB GPU (RTX 4090) w/ bf16, no activation checkpointing
  batch_size_type: frame  # frame | sample
  max_samples: 64  # max sequences per batch if use frame-wise batch_size. we set 32 for small models, 64 for base models
  num_workers: 16

optim:
  epochs: 686
  learning_rate: 7.5e-5
  num_warmup_updates: 20000  # warmup updates
  grad_accumulation_steps: 6  # 6 x 3200 = 19200 frames/update; cheaper than activation checkpointing here
  max_grad_norm: 1.0  # gradient clipping
  bnb_optimizer: False  # use bnb 8bit AdamW optimizer or not (saves ~4 GiB of optimizer state)

model:
  name: WavTTS_Uncond_Large_RYaRN  # model name
  backbone: DiT
  cfm:
    prediction: x_pred         # flow | x_pred
    loss_space: v         # flow | v   (v is typical for x_pred mode)
    t_sampling: logistic_normal      # uniform | logistic_normal
    P_mean: -0.8
    P_std: 0.8
    t_eps: 0.02
    latents_scale: 9.0
    use_aux_mel_loss: True
    aux_mel_loss_weight: 0.05
    sample_rate: 16000
    state_null_prob: 0.5    # prob a sample is labelled null; the rest are clean
    p_mix: 0.5              # among null samples, prob of the mixing augmentation, so
                            # p_null = 0.5*p_clean + 0.5*p_mixed. Any p_mix < 1 keeps
                            # CFG self-extinguishing in the clean region; p_mix sets
                            # how deep into it the guidance survives
    p_concat: 0.5           # among mixed: concat (temporal switch) vs overlap
    mix_lambda_range: [0.3, 0.7]
    concat_point_range: [0.3, 0.7]
    concat_xfade_ms: 20
  arch:
    dim: 1152
    depth: 28
    heads: 16
    dropout: 0.0
    ff_mult: 4
    qk_norm: null  # null | rms_norm
    pe_attn_head: null
    attn_backend: torch  # torch | flash_attn
    attn_mask_enabled: False
    checkpoint_activations: False  # recompute activations and save memory for extra compute
    use_audio_proj: True
    audio_proj_dim: 1024
    audio_proj_hidden: 768
    # length extrapolation — Randomized YaRN (arXiv:2606.23687) + entropy invariance.
    # Training data tops out at 2979 frames (29.8 s). s=4 matches rpe_gamma: the
    # augmentation puts the longest clip's positions out to 4 * 2979 = 11916, so the
    # YaRN spectrum is tuned for the range actually trained on (4 * 3000 = 120 s).
    # Inference at s' > 4 reaches past that. rope_type: default disables all of it.
    # attention shape. "full" is ordinary bidirectional attention. The two causal modes
    # exist for the NoPE experiment: with rope_type: none the causal mask is the only
    # thing that breaks permutation symmetry, so it becomes the position signal
    # (Kazemnejad et al. 2023). "bidir_causal" gives every head both directions and sums
    # them (same attention FLOPs as full); "bidir_causal_split" gives half the heads the
    # past and half the future (half the FLOPs).
    attn_mode: full          # full | bidir_causal | bidir_causal_split
    rope_type: yarn          # default | yarn | none
    yarn_scale: 4.0          # s during training; set s' at inference. Keep in step with
                             # rpe_gamma -- that is what decides the positions s must cover
    yarn_native_ctx: 3000    # frames (30 s) the architecture should cover unaided
    yarn_alpha: 1.0          # dims turning < alpha times over native_ctx: full interpolation
    yarn_beta: 32.0          # dims turning > beta times: pure extrapolation
    rpe_gamma: 4.0           # per-sample stretch bound: L_t ~ U[n, n*gamma], drawn fresh
                             # every forward. gamma=1 disables. Not a curriculum: every
                             # batch spans contiguous through 4x from the first update
    logn_ref_len: 500        # entropy-invariant scaling reference, clamped at 1 so it only
                             # sharpens: 1.0 up to 5 s, 1.29 at 30 s, 1.51 at 120 s. At 3000
                             # every training clip sat below 1 and it never sharpened at all
  waveform:
    target_sample_rate: 16000
    wav_frame_len: 160    # 160 @16k = 100Hz
    target_rms: 0.1       # per-utterance loudness normalization at load time; 0 disables

ckpts:
  logger: tensorboard  # wandb | tensorboard | null
  log_per_updates: 100  # scalar (loss/lr) logging interval in updates
  log_samples: True  # generate fixed-seed samples per save checkpoint (audio + mel + metrics to tensorboard)
  log_samples_seeds: [0, 1, 2, 3]  # same seeds every checkpoint, clips comparable across training
  # one length per seed, fixed across checkpoints. 5s is the bulk of the data, 15s the
  # p95, 30s the training maximum, and 60s is the first genuine length extrapolation.
  # 60s sits inside the trained spectrum (s=4 covers 120s), so it tests the model, not
  # the frequencies. Past 120s needs s' > 4; do that offline with sample_uncond.py
  # --yarn_scale rather than inside the training loop.
  log_samples_secs: [5, 15, 30, 60]
  spk_ckpt_path: null  # ECAPA (wavlm_large) ckpt for gen/spk_sim_self; null disables the metric
  save_per_updates: 10000  # numbered checkpoint every ~3.5 h on one 4090
  keep_last_n_checkpoints: 5  # -1 to keep all, 0 to not save intermediate, > 0 to keep last N checkpoints
  last_per_updates: 2500  # model_last.pt + fixed-seed samples/mel/metrics ride on this interval
  exp_name: ${model.name}_${datasets.name}
  save_dir: ckpts/${model.name}_${datasets.name}
